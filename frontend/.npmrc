# 前端依赖用 `pnpm install --ignore-workspace` 独立安装（根目录的 pnpm-workspace.yaml 不属于本项目构建）。
# 关闭 pnpm v11 的运行前依赖校验，否则 `pnpm build` / `pnpm tauri:dev` 会尝试按 workspace 重装并清空 node_modules。
verify-deps-before-run=false
